비동기 함수 붐() {
    던지기 "TASK_BOOM";
}

변수 t = 붐();
변수 caught = 거짓;
시도 {
    대기최대(t, 1000);
} 잡기(msg, info) {
    caught = 참;
    단언(문자포함(msg, "TASK_BOOM"), "대기최대 실패 메시지 누락");
    단언(길이(info.코드) > 0, "대기최대 실패 코드 누락");
} 마침 {}

단언(caught, "대기최대 실패 catch 미발생");
단언(작업실패(t), "작업실패 상태 실패");
단언(작업상태(t) == "실패", "작업상태 실패");
단언(문자포함(작업오류(t), "TASK_BOOM"), "작업오류 메시지 실패");
단언(길이(작업오류코드(t)) > 0, "작업오류코드 실패");
