형식 기반 {
    행동 값() {
        반환 3;
    }
}

변수 네임 = {모음: {기반: 기반}};

형식 확장 물려받기 네임.모음.기반 {
    행동 두배() {
        반환 메서드호출(자기, "값") * 2;
    }
}

변수 e = 인스턴스생성(확장);
단언(메서드호출(e, "값") == 3, "점표기 부모 상속 실패");
단언(메서드호출(e, "두배") == 6, "점표기 부모 기반 메서드 호출 실패");
