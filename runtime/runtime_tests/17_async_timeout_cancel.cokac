비동기 함수 느림() {
    대기밀리초(120);
    반환 1;
}

변수 timedOut = 거짓;
시도 {
    변수 t = 느림();
    대기최대(t, 10);
} 잡기(msg, info) {
    timedOut = 참;
    단언(문자포함(msg, "시간 초과"), "대기최대 시간초과 메시지 실패");
    단언(길이(info.코드) > 0, "대기최대 오류코드 실패");
} 마침 {}
단언(timedOut, "대기최대 시간초과 미발생");

변수 t2 = 느림();
단언(작업취소(t2), "작업취소 실패");
단언(작업실패(t2), "작업취소 후 작업실패 기대");
단언(작업상태(t2) == "취소", "작업취소 상태 실패");
단언(작업오류코드(t2) == "E_TASK_CANCELLED", "작업취소 오류코드 실패");
