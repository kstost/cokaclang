비동기 함수 느림(v) {
    대기밀리초(60);
    반환 v;
}

변수 a = 느림(1);
변수 b = 느림(2);
단언(비동기큐길이() >= 1, "비동기큐길이 초기값 실패");

변수 all = 작업모두([a, b]);
변수 rs = 대기최대(all, 3000);
단언(길이(rs) == 2, "작업모두 결과 길이 실패");

변수 st = 비동기통계();
단언(st.누적완료 >= 2, "비동기통계 누적완료 실패");
단언(st.루프틱 >= 1, "비동기통계 루프틱 실패");
