변수 p = "edge_zero.txt";
파일쓰기(p, "");
단언(파일크기(p) == 0, "0바이트 파일크기 실패");
파일쓰기줄들(p, ["a", "", "c"]);
변수 ls = 파일읽기줄들(p);
단언(길이(ls) == 3, "빈줄 포함 줄개수 실패");
단언(ls[1] == "", "빈줄 유지 실패");
단언(파일삭제(p), "파일 삭제 실패");
