형식 카운터 {
    속성 값 = 0;

    행동 증가() {
        자기.값 = 자기.값 + 1;
        반환 자기.값;
    }

    행동 읽기() {
        반환 자기.값;
    }
}

변수 a = 인스턴스생성(카운터);
변수 b = 인스턴스생성(카운터);

단언(메서드호출(a, "증가") == 1, "a 증가 1 실패");
단언(메서드호출(a, "증가") == 2, "a 증가 2 실패");
단언(메서드호출(b, "읽기") == 0, "인스턴스 상태 격리 실패");
