형식 타이머 {
    만들기(시작값) {
        자기.현재 = 시작값;
    }

    행동 틱() {
        자기.현재 = 자기.현재 + 1;
        반환 자기.현재;
    }
}

변수 t = 인스턴스생성(타이머, [10]);
단언(메서드호출(t, "틱") == 11, "선언 없는 속성 할당 실패");
단언(메서드호출(t, "틱") == 12, "선언 없는 속성 상태 유지 실패");
