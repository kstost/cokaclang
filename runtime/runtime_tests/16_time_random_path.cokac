변수 t1 = 현재시간();
대기밀리초(20);
변수 t2 = 현재시간();
단언(t2 >= t1, "현재시간 단조성 실패");

변수 ts = 시간문자열();
단언(길이(ts) > 0, "시간문자열 실패");

변수 r = 난수();
단언(r >= 0 그리고 r <= 1, "난수 범위 실패");

변수 ri = 난수정수(3, 9);
단언(ri >= 3 그리고 ri <= 9, "난수정수 범위 실패");

변수 n = 경로정규화("./a/../b.txt");
단언(문자포함(n, "b.txt"), "경로정규화 실패");

변수 rel = 상대경로("/tmp", "/tmp/a/b");
단언(문자포함(rel, "a"), "상대경로 실패");
