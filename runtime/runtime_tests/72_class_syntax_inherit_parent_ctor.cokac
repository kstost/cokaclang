형식 사람 {
    속성 이름 = "";

    만들기(이름) {
        자기.이름 = 이름;
    }

    행동 이름읽기() {
        반환 자기.이름;
    }
}

형식 개발자 물려받기 사람 {
    행동 소개() {
        반환 "개발자:" + 자기.이름;
    }
}

변수 d = 인스턴스생성(개발자, ["철수"]);
단언(메서드호출(d, "이름읽기") == "철수", "부모 생성자 상속 호출 실패");
단언(메서드호출(d, "소개") == "개발자:철수", "자식 메서드 실패");
