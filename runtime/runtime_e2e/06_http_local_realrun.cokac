변수 s = 서버열기(19123, "127.0.0.1");
변수 a = 비동기요청받기(s);
변수 r = 비동기가져오기요청("http://127.0.0.1:19123/ok", {로컬차단: 거짓, 재시도: 0, 최대시간초: 3, 연결시간초: 1});
변수 req = 대기최대(a, 3000);
응답보내기(req.연결, 200, "E2E_HTTP_OK");
연결닫기(req.연결);
변수 resp = 대기최대(r, 3000);
출력 응답본문(resp);
연결닫기(s);
