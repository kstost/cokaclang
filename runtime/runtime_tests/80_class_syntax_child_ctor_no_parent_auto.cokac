형식 부모 {
    만들기() {
        자기.값 = 10;
    }
}

형식 자식 물려받기 부모 {
    만들기() {
        // 의도적으로 부모 초기화를 호출하지 않음
    }

    행동 값읽기() {
        반환 자기.값;
    }
}

변수 z = 인스턴스생성(자식);
단언(메서드호출(z, "값읽기") == 없음, "자식 생성자에서 부모 초기화 자동 호출되면 안 됩니다.");
