변수 x = 1;
함수 f() {
    변수 x = 2;
    반환 x;
}
단언(f() == 2, "함수 내부 그림자 변수 실패");
단언(x == 1, "외부 변수 오염 실패");

함수 makeAdder(base) {
    함수 add(v) {
        반환 base + v;
    }
    반환 add;
}
변수 add10 = makeAdder(10);
단언(add10(5) == 15, "클로저 캡처 계산 실패");
