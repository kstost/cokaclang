변수 이름 = "COKAC";
변수 숫자들 = [1, 2, 3, 4, 5];

함수 합계(값들) {
    변수 합 = 0;
    동안 (변수 i = 0; i < 길이(값들); i = i + 1) {
        합 = 합 + 값들[i];
    }
    반환 합;
}

변수 결과 = 합계(숫자들);
출력 ("안녕, " + 이름 + "!");
출력 ("배열 합계: " + 문자열(결과));
