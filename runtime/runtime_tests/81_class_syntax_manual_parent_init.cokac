형식 부모 {
    행동 부모초기화(v) {
        자기.값 = v;
    }

    행동 값읽기() {
        반환 자기.값;
    }
}

형식 자식 물려받기 부모 {
    만들기(v) {
        메서드호출(자기, "부모초기화", [v]);
    }
}

변수 c = 인스턴스생성(자식, [33]);
단언(메서드호출(c, "값읽기") == 33, "수동 부모 초기화 호출 실패");
