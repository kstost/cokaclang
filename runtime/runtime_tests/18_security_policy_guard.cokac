변수 blocked = 거짓;
시도 {
    가져오기요청("https://example.com", {HTTPS검증: 거짓, 재시도: 0, 최대시간초: 1, 연결시간초: 1});
} 잡기(msg, info) {
    blocked = 참;
    단언(문자포함(msg, "보안 정책 위반"), "보안정책 메시지 실패");
    단언(길이(info.코드) > 0, "보안정책 오류코드 실패");
} 마침 {}

단언(blocked, "HTTPS 검증 비활성 보안정책 미동작");
