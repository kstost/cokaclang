형식 기계 {
    행동 타입() {
        반환 "기계";
    }

    행동 코드() {
        반환 7;
    }
}

형식 로봇 물려받기 기계 {
    행동 타입() {
        반환 "로봇";
    }
}

변수 r = 인스턴스생성(로봇);
단언(메서드호출(r, "타입") == "로봇", "오버라이드 메서드 실패");
단언(메서드호출(r, "코드") == 7, "부모 메서드 상속 실패");
