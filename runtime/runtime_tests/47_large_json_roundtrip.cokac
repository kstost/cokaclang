변수 arr = [];
동안 (변수 i = 0; i < 400; i = i + 1) {
    배열추가(arr, {id: i, name: "n" + 문자열(i), ok: (i % 2 == 0)});
}
변수 s = 자료문자열화(arr);
변수 back = 자료파싱(s);
단언(길이(back) == 400, "대형 JSON 길이 실패");
단언(back[123].id == 123, "대형 JSON 내용 실패");
